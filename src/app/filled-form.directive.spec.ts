import { FilledFormDirective } from './filled-form.directive';

describe('FilledFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FilledFormDirective();
    expect(directive).toBeTruthy();
  });
});
